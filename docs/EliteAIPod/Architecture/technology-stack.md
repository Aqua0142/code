---
project_id: a190ad94-de12-4f87-b68c-5b3e244afc1c
stage: architecture
agent: solution_architect
deliverable: Technology-Stack
generated_at: "2026-08-20T11:56:07.206665+00:00"
---

# Technology Stack — Pharmacy Inventory Search

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-20T11:56+00:00.

| Category | Technology | Version | Rationale |
|---|---|---|---|
| frontend | React 18 | 18.3 | React 18 with hooks provides a declarative, component-based model ideally suited to the real-time search UI with debounced input, live result list, and multiple system states (loading/empty/error). Its ecosystem (React Query for async state) makes latency-sensitive data fetching patterns straightforward. WCAG 2.1 AA compliance is well-supported through accessible component patterns. |
| backend | FastAPI | 0.115 | FastAPI delivers high-throughput async Python with automatic OpenAPI docs, Pydantic validation (preventing SQL injection via typed query params), and sub-millisecond handler overhead — keeping total round-trip well under the 500ms NFR. Python's SQLAlchemy ORM integrates directly with PostgreSQL ILIKE queries. |
| database | PostgreSQL | 16 | PostgreSQL natively supports ILIKE for case-insensitive partial matching, pg_trgm extension for GIN/trigram indexes enabling sub-50ms searches over 1,000 records, and pgcrypto for encryption-at-rest of sensitive ePHI fields. It is a proven, HIPAA-compatible SQL database with strong community support. |
| hosting | Docker + internal server | 24 | Containerized deployment on the internal pharmacy network satisfies the security assumption (no external exposure) and the 99.9% availability NFR. Docker Compose orchestrates the API and database containers with defined health checks and restart policies. |
| build_tooling | Vite | 5.4 | Vite provides fast HMR for developer productivity and produces an optimized static bundle served by the FastAPI app or a lightweight static server. No separate CDN or SPA server needed for this internal tool. |

**frontend — alternatives considered:** Vue 3 — viable but smaller hiring pool in healthcare/pharmacy contexts, Vanilla JS — insufficient structure for maintaining loading/error/empty states cleanly
**backend — alternatives considered:** Django REST Framework — higher overhead, ORM query builder less transparent for ILIKE tuning, Node.js/Express — viable but adds a second runtime language with no clear advantage for this use case
**database — alternatives considered:** MySQL 8 — lacks pg_trgm trigram indexing for ILIKE; full-text search less ergonomic, SQLite — no concurrent write support and limited encryption-at-rest tooling
**hosting — alternatives considered:** Bare-metal Python process — no isolation or reproducible deployments, Cloud PaaS (e.g., AWS ECS) — unnecessary for internal-only, small-user-count deployment; adds ePHI data residency complexity
**build_tooling — alternatives considered:** Create React App (CRA) — deprecated, slow build pipeline, Webpack 5 — verbose configuration overhead for a simple SPA
