---
project_id: a190ad94-de12-4f87-b68c-5b3e244afc1c
stage: architecture
agent: solution_architect
deliverable: Architecture-Diagrams
generated_at: "2026-08-20T11:56:16.490608+00:00"
---

# Architecture Diagrams — Pharmacy Inventory Search

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-20T11:56+00:00.
> Mermaid diagrams render natively on GitHub.

## Pharmacy Medication Search — Container Diagram (C4 Level 2)

Shows the three main containers: the React SPA served to the pharmacist's browser, the FastAPI backend processing search requests, and the PostgreSQL database storing encrypted medication records. All communication uses HTTPS/TLS.
```mermaid
C4Container
  title Pharmacy Medication Search — Containers
  Person(pharmacist, "Pharmacist", "Searches medication inventory")
  Container_Boundary(app, "Pharmacy Medication Search") {
    Container(spa, "MedicationSearchSPA", "React 18 / TypeScript", "Single-page app: search bar, result cards, loading/empty/error states")
    Container(api, "MedicationSearchAPI", "FastAPI / Python 3.12", "REST API: medication search endpoint with input validation and ILIKE queries")
    ContainerDb(db, "MedicationDatabase", "PostgreSQL 16", "Stores medication records; GIN trigram index on name; encryption-at-rest")
  }
  Container_Ext(nginx, "nginx", "Reverse Proxy", "Terminates TLS 1.2+, serves static SPA assets, proxies API requests")
  Rel(pharmacist, nginx, "Uses browser", "HTTPS")
  Rel(nginx, spa, "Serves static assets", "File")
  Rel(nginx, api, "Proxies /api requests", "HTTP")
  Rel(spa, nginx, "Calls search API", "HTTPS/REST")
  Rel(api, db, "Queries medications", "PostgreSQL wire protocol")
```

## Medication Search — End-to-End Data Flow

Traces the data transformation path from raw user keystrokes through debounce, API validation, database query, and JSON serialization back to rendered UI components. Highlights where input sanitization and type validation occur.
```mermaid
flowchart LR
  A["User Keystroke\n(raw string)"] -->|onChange event| B["Debounce Hook\n300ms window"]
  B -->|query string >= 3 chars| C["API Client\nfetch /api/v1/medications/search?q=..."]
  C -->|HTTP GET + TLS| D["FastAPI Route Handler\nPydantic validates q param"]
  D -->|validated SearchQuery| E["Medication Service\nbusiness logic layer"]
  E -->|search_term| F["Medication Repository\nparameterized ILIKE query"]
  F -->|SQL: name ILIKE '%term%'| G["PostgreSQL GIN Index\ntrigram scan"]
  G -->|rows: id, name, stock_level| F
  F -->|ORM Medication objects| E
  E -->|List[MedicationDTO]| D
  D -->|JSON: {data: [...]}| C
  C -->|parsed response| H["React Query Cache"]
  H -->|data / isLoading / error| I["SearchResultsList Component"]
  I -->|renders| J["MedicationResultCard\nname + stock_level"]
```

## Pharmacy Medication Search — Deployment Topology

Shows the Docker Compose deployment on an internal pharmacy server within the VLAN-isolated network. nginx terminates TLS and serves as reverse proxy for the FastAPI API container and static SPA assets.
```mermaid
graph TB
  subgraph Browser ["Pharmacist Workstation"]
    BW[Web Browser]
  end
  subgraph Server ["Internal Pharmacy Server (Docker Host)"]
    subgraph DockerNetwork ["Docker Bridge Network: pharma-net"]
      NG[nginx:443\nTLS Termination\nStatic SPA + Reverse Proxy]
      API[pharma-api:8000\nFastAPI Application\nPython 3.12]
      DB[pharma-db:5432\nPostgreSQL 16\nEncrypted at Rest]
    end
    VOL[(pg_data volume\nEncrypted filesystem)]
  end
  subgraph Network ["Internal VLAN"]
    FW[Firewall / Access Control]
  end
  BW -->|HTTPS :443| FW
  FW -->|HTTPS| NG
  NG -->|HTTP| API
  API -->|PostgreSQL wire| DB
  DB --- VOL
```

## Medication Inventory — Entity Relationship Diagram

Shows the single Medication entity and all its fields. No relationships exist in the current sprint scope; nullable extended fields (ndc, strength, form, manufacturer) are placeholders to reduce future migration risk.
```mermaid
erDiagram
  MEDICATION {
    UUID id PK "gen_random_uuid()"
    VARCHAR_500 name "NOT NULL, GIN trigram indexed"
    INTEGER stock_level "NOT NULL, >= 0"
    VARCHAR_20 ndc "nullable"
    VARCHAR_100 strength "nullable"
    VARCHAR_100 form "nullable"
    VARCHAR_255 manufacturer "nullable"
    TIMESTAMPTZ created_at "NOT NULL, default NOW()"
    TIMESTAMPTZ updated_at "NOT NULL, default NOW()"
  }
```

## Pharmacy Medication Search — Module Interaction

Illustrates internal module boundaries within the FastAPI backend (routes → services → repositories → models) and the React frontend component hierarchy. Shows synchronous data flow and dependency direction.
```mermaid
graph TD
  subgraph Frontend ["React SPA"]
    SC[SearchBar Component]
    RC[MedicationResultCard Component]
    SS[SystemStateDisplay Component]
    AH[useSearchMedications Hook]
    AC[api/medicationsClient.ts]
  end
  subgraph Backend ["FastAPI Application"]
    RT[routes/medications.py]
    SV[services/medication_service.py]
    RP[repositories/medication_repository.py]
    MD[models/medication.py]
  end
  subgraph DB ["PostgreSQL"]
    TBL[(medications table)]
  end
  SC -->|user input| AH
  AH -->|debounced query| AC
  AH -->|results| RC
  AH -->|state| SS
  AC -->|GET /api/v1/medications/search| RT
  RT -->|validated query| SV
  SV -->|search params| RP
  RP -->|ILIKE query| MD
  MD -->|SQL| TBL
  TBL -->|rows| MD
  MD -->|ORM objects| RP
  RP -->|domain objects| SV
  SV -->|response DTOs| RT
```

## Sequence Diagrams

### Real-Time Medication Search — Happy Path (US-001, US-002)

Shows the complete flow from pharmacist keystrokes through debounce, API call, PostgreSQL ILIKE query, and result rendering in the SPA including the loading state transition.
```mermaid
sequenceDiagram
  participant P as Pharmacist
  participant SPA as React SPA
  participant API as FastAPI
  participant DB as PostgreSQL

  P->>SPA: Types "met" (3+ chars)
  SPA->>SPA: Debounce 300ms
  SPA->>SPA: Show loading indicator (aria-live)
  SPA->>API: GET /api/v1/medications/search?q=met
  API->>API: Validate q param (Pydantic)
  API->>DB: SELECT id, name, stock_level FROM medications WHERE name ILIKE '%met%' LIMIT 50
  DB->>DB: GIN trigram index scan
  DB-->>API: [{id, name, stock_level}, ...]
  API-->>SPA: 200 {data: [...], meta: {result_count: 5}}
  SPA->>SPA: Hide loading indicator
  SPA->>P: Render MedicationResultCard list with name + stock_level
```

### Empty State — No Medications Found (US-004)

Shows the flow when a valid 3+ character query returns zero results from the database, triggering the 'No medications found' empty state in the SPA.
```mermaid
sequenceDiagram
  participant P as Pharmacist
  participant SPA as React SPA
  participant API as FastAPI
  participant DB as PostgreSQL

  P->>SPA: Types "xyz" (no matching medications)
  SPA->>SPA: Debounce 300ms
  SPA->>SPA: Show loading indicator
  SPA->>API: GET /api/v1/medications/search?q=xyz
  API->>API: Validate q param
  API->>DB: SELECT ... WHERE name ILIKE '%xyz%'
  DB-->>API: [] (empty result set)
  API-->>SPA: 200 {data: [], meta: {result_count: 0}}
  SPA->>SPA: Hide loading indicator
  SPA->>P: Render 'No medications found' empty state message (aria-live)
```

### Error State — Backend Unavailable (US-004)

Shows the error handling flow when the FastAPI backend is unreachable or returns a 5xx response, resulting in a user-friendly error message displayed in the SPA.
```mermaid
sequenceDiagram
  participant P as Pharmacist
  participant SPA as React SPA
  participant API as FastAPI

  P->>SPA: Types "asp" (3+ chars)
  SPA->>SPA: Debounce 300ms
  SPA->>SPA: Show loading indicator
  SPA->>API: GET /api/v1/medications/search?q=asp
  API-->>SPA: 500 Internal Server Error (or network timeout)
  SPA->>SPA: Hide loading indicator
  SPA->>SPA: Set error state (aria-live region)
  SPA->>P: Render user-friendly error message: 'Unable to reach medication database. Please try again.'
```

## Pharmacy Medication Search — System Context (C4 Level 1)

Shows the pharmacy medication search system in context with its primary user (Pharmacist) and the internal network boundary that provides security isolation. No external systems are integrated.
```mermaid
C4Context
  title Pharmacy Medication Search — System Context
  Person(pharmacist, "Pharmacist", "Searches medication inventory to check stock levels and make dispensing decisions")
  System(pharma_search, "Pharmacy Medication Search", "Single-page web application for real-time medication inventory search. Displays medication names and stock levels.")
  System_Ext(internal_network, "Internal Pharmacy Network", "VLAN-isolated network providing access control boundary for ePHI data. No public internet exposure.")
  Rel(pharmacist, pharma_search, "Searches medications", "HTTPS")
  Rel(pharma_search, internal_network, "Runs within", "Network boundary")
```

