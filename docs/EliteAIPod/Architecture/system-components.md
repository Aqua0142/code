---
project_id: a190ad94-de12-4f87-b68c-5b3e244afc1c
stage: architecture
agent: solution_architect
deliverable: System-Components
generated_at: "2026-08-20T11:56:09.476590+00:00"
---

# System Components — Pharmacy Inventory Search

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-20T11:56+00:00.

## MedicationSearchSPA (service)

React 18 single-page application that provides the real-time medication search UI. Renders the search bar with debounced input, result cards displaying medication name and stock level, and loading/empty/error system states.

**Responsibilities:**
- Render search bar input with WCAG 2.1 AA compliant label, focus ring, and aria attributes
- Debounce user input and trigger API calls only when query length >= 3 characters
- Display loading indicator while API request is in-flight
- Render medication result cards with name and stock_level
- Display 'No medications found' empty state on zero results
- Display user-friendly error message on network or server error
**Depends on:** MedicationSearchAPI**Communication protocol:** HTTPS/REST

## MedicationSearchAPI (service)

FastAPI application exposing the medication search endpoint. Handles input validation, executes case-insensitive ILIKE queries against the medications table, and returns structured JSON responses within the 500ms SLA.

**Responsibilities:**
- Expose GET /api/v1/medications/search endpoint
- Validate and sanitize the query parameter to prevent SQL injection
- Enforce minimum query length of 3 characters at the API layer
- Execute parameterized ILIKE query against PostgreSQL
- Return medication name and stock_level in JSON response
- Return appropriate HTTP error responses for invalid input or server errors
**Depends on:** MedicationDatabase**Communication protocol:** HTTPS/REST
**Port:** 8000
## MedicationDatabase (database)

PostgreSQL 16 instance storing the medications table. Configured with encryption-at-rest via pgcrypto and pg_trgm trigram indexes on the name column for fast ILIKE searches.

**Responsibilities:**
- Persist medication records with id, name, stock_level, and nullable extended fields
- Provide GIN trigram index on name column for sub-50ms ILIKE query execution
- Enforce encryption-at-rest using pgcrypto or filesystem-level encryption
- Serve read queries from the MedicationSearchAPI within query latency budget
**Communication protocol:** PostgreSQL wire protocol
**Port:** 5432
